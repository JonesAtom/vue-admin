export default {
    updateUserInfo({commit, state}) {
        commit('updateUserInfo')
    },

    updateStoreInfo({commit, state}) {
        commit('updateStoreInfo')
    },

    updateName({commit, state}) {
        commit('updateName')
    }
}